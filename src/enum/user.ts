export enum AccountTier {
    Tier1 = 1,
    Tier2 = 2,
    Tier3 = 3,
}

export enum AccountStatus {
    PENDING = "pending",
    VERIFYING = "verifying",
    ACTIVE = "active",
    DISABLED = "disabled",
}
